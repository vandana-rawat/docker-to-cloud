# docker-to-cloud
A cloud-based application developed with Flask and MongoDB, packaged with Docker, validated locally using Docker Compose and Minikube, and orchestrated through Kubernetes on AWS EKS with replica scaling, rolling deployments, and pod health checks.
